{% extends "::base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <h2>{{ title }}</h2>
    {% if lien is not null %}
        {{ lien }}
    {% else %}
        {{ form(form) }}
    {% endif %}
{% endblock %}
